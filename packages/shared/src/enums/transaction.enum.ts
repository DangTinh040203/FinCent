export enum TransactionType {
  INCOME = 'INCOME',
  EXPENSE = 'EXPENSE',
  TRANSFER = 'TRANSFER',
}

export enum TransactionSource {
  MANUAL = 'MANUAL',
  AI_DRAFT = 'AI_DRAFT',
  RECURRING = 'RECURRING',
  IMPORT = 'IMPORT',
}
